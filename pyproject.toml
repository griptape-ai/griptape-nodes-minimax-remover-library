[project]
name = "griptape-nodes-minimax-remover-library"
version = "0.1.0"
description = "MiniMax-Remover integration for Griptape Nodes - AI-powered video object removal"
authors = [
    {name = "Your Name", email = "you@example.com"}
]
readme = "README.md"
requires-python = "~=3.12"
dependencies = [
]

[tool.uv.sources]
griptape-nodes-engine = { git = "https://github.com/griptape-ai/griptape-nodes-engine", rev="latest"}

[dependency-groups]
dev = ["pyright>=1.1.396", "pytest-asyncio>=0.21.0", "ruff>=0.8.0", "pytest", "griptape-nodes-engine"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
